# AI 辅助开发说明

## 1. 使用的 AI 工具与涉及模块

本项目使用 **GitHub Copilot（VS Code）** 进行代码分析、实现建议、文件修改和构建验证。

| AI 工具 | 使用模块 | 主要用途 |
| --- | --- | --- |
| GitHub Copilot | `src/components/Home.vue` | 项目工时统计、ECharts 柱状图、表格展示、删除事件、权限列控制、响应式布局 |
| GitHub Copilot | `src/stores/project.ts` | 创建 Pinia store、保存成员数据、实现删除和 ID 重排 |

## 2. AI 生成的代码与人工修改

### 根据需求进行的修改和取舍

- 根据页面截图移除了右侧树状图，只保留表格和柱状图。
- 将 `Options` 列从固定数组改为计算属性，使普通用户连表头和整列都不渲染。
- 将柱状图横轴标签设置为全部显示，并旋转标签减少重叠。
- 根据手机端显示问题增加 `@media (max-width: 768px)`，让表格和图表在窄屏下改为上下排列。
- 保留桌面端表格和柱状图同一行的要求。
- 保留现有 `Login.vue` 的人工调整，例如登录按钮居中样式。
- 对每次修改执行类型检查和生产构建，确认 AI 生成代码可以正常编译。

## 3. 最难和最满意的部分

### 最难的部分

最难的是让同一份 Pinia 数据同时驱动表格、累计工时计算和 ECharts 图表。删除成员后不仅要更新表格，还要让项目分组结果和柱状图重新计算；同时还要处理 ID 重排，避免删除中间成员后出现编号断档。

另一个需要反复调整的部分是页面布局：桌面端要求表格和图表同排，而手机端需要避免固定宽度导致内容挤压，因此最终采用桌面 flex 横排、移动端单列和表格横向滚动的组合方案。

### 最满意的部分

最满意的是数据流比较清晰：

```text
Pinia store
    -> projectHours computed
    -> ECharts option
    -> 表格和柱状图自动更新
```

管理员删除数据后，store、表格、项目累计工时和柱状图能够保持同步；普通用户不会看到删除列，登录和权限展示也形成了完整的基础流程。

## 4. 对 AI 协作开发的理解

**个人打下最坚实的地基，并紧握项目的主航向；AI 则扮演技术精湛且不知疲倦的建造队员，按你的标准去铺设砖瓦、填充功能细节。**

在这种协作下，你不再需要把精力消耗在重复写模板代码、调语法格式等繁琐体力活上。你只需负责业务逻辑的定调、关键决策与代码审查，而 AI 负责将你的设计快速落地。它极大地放大了你的设计意图，让你一个人就能拥有一个高效输出的团队，把创意以最快速度转化为高品质的代码。
